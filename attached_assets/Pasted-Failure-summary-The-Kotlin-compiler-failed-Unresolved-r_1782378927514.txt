Failure summary
- The Kotlin compiler failed: Unresolved reference: BuildConfig in app/src/main/java/com/nex/peek/ui/PseudocodeFragment.kt (lines reported in the logs: 17, 62, 68).
- The root cause: the generated BuildConfig class is not available under the package referenced by the import (com.nex.peek.BuildConfig). That usually means the module namespace/applicationId differs from the file package, or the BuildConfig generation/namespace is not configured for the app module.

Two safe, actionable fixes (pick one)

1) Preferred: make BuildConfig usage independent of generated class (robust)
- Replace BuildConfig.DEBUG checks with a runtime "isDebug" check derived from the application flags. This avoids relying on the generated BuildConfig package and fixes the compile error immediately.

Patch for PseudocodeFragment.kt (minimal changes)
- Remove: import com.nex.peek.BuildConfig
- Add: import android.content.pm.ApplicationInfo
- Add an isDebugBuild() helper and replace BuildConfig.DEBUG usages.

Suggested code changes (diff-like):

Replace the imports:
- Remove this line:
  import com.nex.peek.BuildConfig

- Add:
  import android.content.pm.ApplicationInfo

Add this helper inside the class (for example right after currentFunc declaration):

private fun isDebugBuild(): Boolean {
    return (requireActivity().application.applicationInfo.flags
            and ApplicationInfo.FLAG_DEBUGGABLE) != 0
}

Then replace occurrences:

withContext(Dispatchers.IO) {
    code = PeekNative.decompileFunction(handle, fn.id)
    if (code.isEmpty() && isDebugBuild()) {
        failReason = PeekNative.getLastError(handle)
    }
}

and

if (code.isEmpty()) {
    if (isDebugBuild() && failReason.isNotEmpty()) {
        b.tvEmpty.text = "Decompilation failed\n\n$failReason"
    } else {
        b.tvEmpty.text = "Decompilation unavailable"
    }
    b.tvEmpty.visibility = View.VISIBLE
} else {
    ...
}

Why this works
- This avoids compile-time dependency on a generated class that may be under a different namespace, and it correctly reflects whether the app is debuggable at runtime.

2) Alternative: fix the Gradle namespace so BuildConfig is generated in com.nex.peek
If you prefer to keep using BuildConfig.DEBUG, make sure the module’s namespace matches the Kotlin package com.nex.peek so the generated BuildConfig class is placed there.

Edit the app module Gradle file:

- Groovy (app/build.gradle):
android {
    namespace "com.nex.peek"   // <- ensure this matches the Kotlin file package
    ...
}

- Kotlin DSL (app/build.gradle.kts):
android {
    namespace = "com.nex.peek"
    ...
}

Also ensure defaultConfig.applicationId (if used) is consistent with what you expect. After changing, clean & rebuild so BuildConfig is regenerated:
./gradlew :app:clean :app:assembleRelease

Why this works
- BuildConfig is generated into the android namespace value. If the namespace differs from the package declared in source files, imports like com.nex.peek.BuildConfig will not resolve.

Which to choose
- Use option 1 (runtime check) if you want a quick, safe compile-time fix that doesn't rely on build config setup.
- Use option 2 if you want to keep BuildConfig usage and are comfortable adjusting the module’s namespace/applicationId to match your package structure.

Verify
- After applying either change run a local build (recommended) to confirm:
  ./gradlew :app:assembleRelease
- Re-run the CI workflow; the Kotlin compile error should be resolved.

If you want, I can:
- Provide an exact patched file content for PseudocodeFragment.kt with option 1 applied.
- Point to the app module Gradle file lines to change if you prefer option 2.