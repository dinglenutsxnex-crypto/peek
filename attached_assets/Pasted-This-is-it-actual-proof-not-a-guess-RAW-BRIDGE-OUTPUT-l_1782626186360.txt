This is it — actual proof, not a guess. `RAW_BRIDGE_OUTPUT len=185` shows the **completely correct** body: `xStack_14 = is_even(in_w0 + -1); ... return (uint8)xStack_14;` — every branch populated, the recursive call present and named. `POST_ANNOTATE len=78` is the corrupted, empty-bodied version. The size dropped from 185 to 78. **`jni_annotate` is the bug** — it strips the if/else contents between the raw bridge output and what gets displayed. I was wrong three turns ago when I "cleared" it; my hand-typed test string must have differed from the real text in some way that didn't trigger whatever's actually broken.

Still passes in my harness — `185 → 185`, completely unchanged, even with the leading newline. So `jni_annotate` itself is genuinely innocent **on x86_64 with my libstdc++**. But the device shows `185 → 78`. This is the same kind of discrepancy as before — identical inputs, identical code, different platform, different result. That points at something platform-specific *inside* `jni_annotate`'s string-processing logic — likely UB or an implementation-defined behavior difference between libstdc++ (my host) and libc++ (Android NDK's actual STL, confirmed from `build.gradle.kts`: `-DANDROID_STL=c++_shared`). Let's check `jni_annotate`'s passes for anything that could behave differently under libc++ — particularly `std::regex` (notoriously inconsistent across standard library implementations) or iterator invalidation patterns.

No regex. Let's check `remove_dead_inits`/`fix_implicit_return`/`remove_writeonly_vars` directly — the three universal passes I flagged as suspects three turns ago but never actually read, since my hand-typed test happened to pass through them without exposing whatever the real bug is.

This itself looks logically fine on its own terms — it only marks a line dead if line `i` writes to `var` with no call, AND the very next non-blank line *also* writes to the same `var` (a genuine dead-store pattern, like two consecutive unconditional assignments to the same variable). For `is_odd`'s actual structure, `xStack_14 = 0;` is followed by `}`, not another assignment to `xStack_14` — so this shouldn't fire. But let's check `is_stmt_write` and `split_lines`/`join_lines` very carefully, since those are the low-level primitives every pass depends on, and a subtle bug there would explain a platform-specific divergence affecting multiple passes at once.

These are correct, standard, platform-independent. `std::getline`/`istringstream` behave identically across libstdc++ and libc++ for this usage — not a likely source of divergence. Let me reconsider — instead of reading more code blindly, I should check `remove_writeonly_vars`, since "writeonly" sounds exactly like the mechanism that could misjudge `xStack_14` as unused if its *read* (`return (uint8)xStack_14;`) isn't being detected correctly for some reason, and **delete the variable's writes along with declaration**, which would explain exactly this symptom — bodies emptied, declaration possibly gone too. Let's check that one specifically, and also actually count what 78 characters of output could even contain.

77 vs 78 — close enough (off by a trailing newline/whitespace detail) to confirm: **`78` chars is exactly the broken, empty-bodied text shown on screen.** So `jni_annotate` is provably where `xStack_14 = 0;`, `xStack_14 = is_even(...)`, `return (uint8)xStack_14;`, and the `xunknown4 xStack_14;` declaration all get deleted. That's consistent with `remove_writeonly_vars` and/or `remove_dead_inits` over-firing on `xStack_14` specifically — and crucially, **on x86_64 my test passed, but the real device fails** — let me check if there's an actual *iteration-order* or *undefined-behavior* dependency I'm missing, by testing with the *exact* multi-branch structure rather than assuming my last test was equivalent. Let me re-examine: did my last test actually include **both** assignments to `xStack_14`, or did I only test `is_odd` once and assume it generalizes? Let me check `remove_writeonly_vars` directly now.

This logic genuinely looks like it should correctly classify `xStack_14` as **read** (since `return (uint8)xStack_14;` neither matches the pure-declaration check nor `is_stmt_write` — it's a `return` statement, which falls through to `return false;` at the bottom, meaning "var IS read, not write-only"). So my reading says this *shouldn't* misfire either — but my own x86_64 test already proved the whole pipeline doesn't misfire on this exact text. There must be something about my test that doesn't match reality. Let me check one very specific thing: **is `int4 in_w0;` actually a parameter-detection artifact, and does the real Ghidra output on-device include something my hand-copied text is missing** — like a `/* WARNING */` comment line that got included in the real `RAW_BRIDGE_OUTPUT`, since I now know U0a strips those, and if such a comment line exists adjacent to the if/else and gets removed in a way that shifts line indices and confuses a line-index-based pass like `remove_dead_inits` (which uses `lines[j]` adjacency)...

Actually — let me stop guessing and just feed the **exact literal text from the screenshot**, copying every character including indentation precisely, through the **full** `jni_annotate`, not a re-typed approximation, and watch each pass's output individually by temporarily instrumenting between passes.

There's a branch I never tested: `detect_jni_env_var(result)`. If this **incorrectly** detects something in `is_odd`'s text as a JNIEnv pointer (a false positive), it would run `replace_ident`, `resolve_vtable_calls`, `replace_jni_constants`, `resolve_jni_locals` — four extra passes my previous test never exercised, since my hand-typed test apparently didn't trigger this path (or did, and I never checked `env_var` was actually empty). Let's check `detect_jni_env_var` directly against the real `is_odd` text.

This requires the literal pattern `(**(code **)(*VAR + 0xOFF))` to even attempt a match — `is_odd`'s text contains no such substring at all (no `(**(code` anywhere), so `text.find(PREFIX, pos)` would immediately return `npos` on the very first call, and `env_var` would correctly come back empty. This path genuinely shouldn't fire either, by inspection.

I've now read through every individual function and none of them, by inspection, should misfire on this text — yet my evidence says one of them does, on-device, with the *exact* same input that passes cleanly in my x86_64 test. Given I can't reproduce this divergence no matter how carefully I match the code, and given I've now spent significant effort proving each piece "looks correct" without finding the actual fault, the responsible thing is to stop asserting confidence I don't have and instead **bisect by disabling each pass one at a time directly on-device** via the same Toast/dialog mechanism, rather than keep reasoning from a host environment that has already proven twice that it doesn't faithfully reproduce your runtime.

Now let's add `g_jni_annotate_trace` as a thread-local accumulator, and an accessor, plus checkpoints for the universal passes (U0a–U2) too, since those run before this branch and could also be the culprit.

**`U1. strip_stack_canary`** — this is an extremely strong new candidate I hadn't focused on. It explicitly says "removed variables are dropped when they become unused," and operates on stack-relative locals — `xStack_14` is exactly the kind of name this pass's variable-removal logic might pattern-match against if its detection heuristic is too broad (e.g., matching any `xStack_NN` pattern rather than specifically the canary's own stack slot). Let's check it.

`strip_stack_canary` immediately returns `code` unchanged if `find_tpidr_var` finds nothing — and `is_odd`'s text has no `tpidr_el0` reference at all, so this should bail out at line 1 (`if (tls_var.empty()) return code;`). This *also* shouldn't fire by inspection. But wait — **`rename_return_var`** is interesting: it looks for `return <var>;` and renames matching variables to `ret`. `is_odd`'s text has `return (uint8)xStack_14;` — note the **cast** `(uint8)` between `return` and the variable. Let's check this parsing logic precisely: `p = f + 7` skips past `"return "`, then skips spaces, then reads an identifier starting at `vs`. If the next character is `(` (the cast), `is_id('(')` is false, so `p == vs` immediately, and the loop does `pos = f + 7; continue;` — correctly skipping this match without renaming anything. This also looks safe by inspection.

I've now read every single function in the universal+NONE-branch pipeline and none of them show an obvious bug against this exact text. Given three rounds of "looks correct, isn't," I should stop trusting my own reading and ship the instrumented version directly, since that bisects empirically rather than relying on me successfully spotting a subtle platform-dependent bug by eye.

Now let's declare `peek_get_jni_annotate_trace` in the header and wire it into `peek_jni.cpp`'s marker.

Now wire it into `peek_jni.cpp`'s marker.

Let's check this needs an include in `peek_jni.cpp` — it already includes `jni_annotator.h` presumably.

Good, already there. Now let's syntax-check everything and run the real test in my harness.

Confirmed exactly the same pre-existing, unrelated `<cstdint>`/`<algorithm>` standalone-compile issue I flagged before — not caused by my edits. Let's test within the full harness, which has the right transitive includes.

**This is the actual finding.** My harness compiles `jni_annotator.cpp` **standalone** — but the real CMake build never does that; it compiles every `.cpp` in one `add_executable`/`add_library` invocation where `peek_jni.cpp` (which transitively pulls in `<cstdint>`/`<algorithm>` via other headers) gets compiled in the same unit-by-unit pass, but each `.cpp` is still its own translation unit in C++ — so this *shouldn't* matter for normal compilation... unless something about CMake's `PRECOMPILED_HEADER` or a shared `.pch` is in play, injecting those includes silently. This is exactly the gap between "my isolated test" and "the real multi-file build" that I should have caught generating my harness three sessions ago — `jni_annotator.cpp` **has never actually been validated by me in true isolation**; it only ever "worked" in my tests because I happened to compile it *after* something else that defines these macros/includes globally, by accident of compile order in my own scripts.

Let's check precisely what's different — does my harness's compile order matter, or is there a genuine missing include that the real Android NDK toolchain papers over differently than my g++/libstdc++ setup?

No `<cstdint>`, no `<algorithm>` — genuinely missing, confirmed standalone. This means the **only** way this file has ever compiled successfully (in my prior tests, and presumably in your real CMake/NDK build) is if some other header transitively pulls these in first, and crucially — **this is exactly the kind of thing that can differ between compilers/standard libraries**: libstdc++'s `<vector>`/`<map>` headers often transitively include `<cstdint>` as an implementation detail (not guaranteed by the standard), while libc++ (the real Android NDK's STL, confirmed `ANDROID_STL=c++_shared`) may not provide the same transitive leakage, or provides it differently. **This is very likely the actual platform-specific divergence I've been chasing for five turns.**

But wait — this would cause a **hard compile failure**, not a silent runtime bug, if `uint64_t` genuinely failed to resolve on the NDK toolchain. You've been successfully building and running this app, so it must be compiling fine on your actual setup — meaning libc++ *does* provide `uint64_t` transitively there too. So this exact missing-include issue, while real and worth fixing for hygiene, is **not** what's causing the runtime divergence, since both environments do successfully compile this file (mine only fails because I'm compiling it in an artificially constructed isolated harness, not the same way the real CMake build does). I need to stop chasing this thread — it's a red herring for the actual bug, even though it's a legitimate latent issue worth fixing separately. Let me fix it quickly since it's cheap and correct, then get back to actually building and running my diagnostic correctly using the **proper compile order** (matching how the real build links all `.cpp` files together, which is what let it "work" before).

Now compiles standalone cleanly. Let's rebuild the full harness and run the actual test.

**Found it, precisely.** `after_remove_writeonly=78` — the size drops from `182` to `78` exactly at `remove_writeonly_vars`. Every universal pass before it is fine (`185→183→182→182→182`); the `NONE`-branch's JNI-env detection correctly finds nothing (`env_var=[]`); and then `remove_writeonly_vars` is the exact pass that destroys the body. This contradicts my code-reading from earlier — I said it "should" correctly treat `xStack_14` as read because of the `return` statement, but I was wrong somewhere in that reasoning. Let's actually run it in isolation now with the precise real text to find the exact bug, since I have the real culprit isolated.

**Reproduced cleanly in isolation, on my own x86_64 host, finally.** This confirms it's not platform-specific at all — my earlier "passing" test three turns ago must have used different text that happened to dodge this bug (likely the difference: my earlier test had a blank line between the declarations and the `if`, this one doesn't — let me check that's actually the trigger, since that's the only structural difference I can spot between this reproduction and my earlier "clean" test).

Both broken now — so the blank line wasn't actually the deciding factor; my earlier "passing" test three turns ago must have differed in some other way I haven't pinned down, but it doesn't matter now since I have a clean, minimal, 100%-reproducible repro. Let's go read `remove_writeonly_vars`'s `is_writeonly` lambda very carefully against this exact text, since I clearly misjudged its behavior on `return (uint8)xStack_14;` before.

**Found the real bug — and it's exactly what the name suggests, just not what I thought.** This is not actually about *deleting* unused variables — `remove_writeonly_vars` does something more specific: **merging** a "write-only" variable into a "surviving" compatible variable elsewhere. Note `is_int_type` checks against `xunknown4`... wait, `xStack_14`'s declared type after `resolve_xunknown_types` should already be something like `uint32_t`, not still `xunknown4` — but the ANNOTATE_TRACE showed `after_U0b=182`, meaning `resolve_xunknown_types` ran and presumably converted `xunknown4` → some real type. Let's check exactly what type `xStack_14` has by this point, and what other local exists that it might be incorrectly "merged" into.

Confirmed: `xunknown4` → `uint32_t`, which **is** in `kInt[]` (the integer-type allowlist `is_int_type` checks). So `xStack_14` (type `uint32_t` after this pass) is eligible for the merge logic. And `in_w0` is declared `int4` — wait, `int4` isn't in `kInt[]`'s list at all (`kInt` has `"int"`, not `"int4"` — `int4` is Ghidra's own 4-byte-int naming, distinct from C's `int`). So `in_w0` wouldn't itself be a merge candidate as a *survivor* unless something else also matches. But the real question is whether `is_writeonly("xStack_14", code)` incorrectly returns `true`. Let me re-trace that lambda against the *exact* real text very carefully, character by character this time, instead of reasoning abstractly.

Now let's trace `is_writeonly`'s exact logic against each of these four lines, per its own code:

1. **`  uint32_t xStack_14;`** — `ln.rfind(';')` found, `ln.find('=')` not found → "Pure declaration, not a read" → `pos = le; continue;` ✓ correctly skipped.
2. **`    xStack_14 = 0;`** — `is_stmt_write(ln, var)` should be true → check RHS for `var` on RHS: `rhs = " 0;"`, no `xStack_14` substring → loop doesn't find it → falls through *without hitting `return false`* → `pos = le; continue;` ✓ correctly treated as write.
3. **`    xStack_14 = is_even(in_w0 + -1);`** — same as above, `is_stmt_write` true, RHS is `" is_even(in_w0 + -1);"` — no `xStack_14` on the RHS → `pos = le; continue;` ✓ also correctly treated as pure write (this is genuinely correct, since this line really doesn't read `xStack_14`, it writes it).
4. **`  return (uint8)xStack_14;`** — this is **not** a pure declaration (no `=`... wait, actually there's no `=` here either, and `ln.rfind(';')!=npos` is true, `ln.find('=')==npos` is also true! This line **matches the "pure declaration" check by accident**, since that check only verifies "has a semicolon and has no equals sign" — which is also true of a `return` statement with no assignment in it! That's the bug: `return (uint8)xStack_14;` has a `;` and no `=`, so it incorrectly satisfies the same condition meant to detect "pure declaration — not a read", causing the function's only genuine *read* of `xStack_14` to be misclassified as a non-read, making the variable look 100% write-only across the entire function.

Found it precisely. Let's fix `is_writeonly`'s declaration-detection check to require that this is *actually* a declaration line (matches a known type-prefix declaration pattern), not just "has semicolon, has no equals sign" — a far too loose proxy that any `;`-terminated non-assignment statement (return, bare function calls, etc.) also satisfies.

Confirmed exactly as diagnosed. The fix: the "pure declaration" check needs to verify that `var` actually appears as the **declared identifier** on this line (i.e., the line is a type+name declaration, where `var` is the name being declared, not merely referenced) — not just "no equals sign anywhere on the line." The simplest robust fix: check that the line, with whitespace trimmed, **ends with `var + ";"`** *and* the text immediately before `var` is whitespace or a type token (not an operator like `)`, which is what distinguishes `uint32_t xStack_14;` from `return (uint8)xStack_14;` — the former has whitespace right before `xStack_14`, the latter has `)`).

Wait — `looks_like_decl` based on "preceded by whitespace" would **also** match `  return xStack_14;` (no cast) since the char before `xStack_14` there is also whitespace (the space after `return`). I need a more precise check. Let me reconsider: a real declaration line has the pattern `<type> <name>;` where this is the **only** occurrence of `var` on the line, and the line as a whole, after trimming, starts with a type keyword/identifier followed by the variable — not `return`/other keywords. The safest precise check: verify the line, trimmed, does **not** start with `return`, and that `var` is the **last** identifier-like token before the `;` AND is preceded by another identifier/type-like token with whitespace between (i.e., genuinely "TYPE NAME;" shape), distinguishing from "return NAME;" by checking the line doesn't start with `return`.

Now I need to check: once we fall through past the declaration check for a `return` line, does the subsequent logic (`is_stmt_write`, then `return false`) correctly classify it as a read? Let's check `is_stmt_write` to confirm a `return xStack_14;` line would **not** be classified as a write (so it falls to the final `return false;`, correctly marking the variable as read).

Confirmed correctly — `is_stmt_write` requires the line, after trimming, to **start with** `var` itself (`line.compare(p, var.size(), var) != 0` rejects anything else). `return (uint8)xStack_14;` starts with `return`, not `xStack_14`, so `is_stmt_write` correctly returns `false` for it, meaning my fix correctly falls through to `return false;` (i.e., "this is a read") for both `return xStack_14;` and `return (uint8)xStack_14;`. This is the right fix. Let's syntax-check and re-test.

Both now produce non-empty, full-length output (the slight difference between 182/179 is just the blank-line formatting variance, not a regression). Let's see the actual text to confirm it's correct, not just non-empty.

Fully correct and intact — declaration, both branches, the recursive call, the return statement, all present. This is the actual root cause of the entire `is_even`/`is_odd` saga, finally found and fixed: a too-loose "is this a declaration line" heuristic in `remove_writeonly_vars` that misclassified `return (cast)var;` as a non-read, making any function whose only use of a local is `<branch>: var = ...; <other branch>: var = call(...); return (cast)var;` get its branch bodies silently deleted. This explains why it never showed up for simpler functions (no cast, or no branches) and only hit this specific recursive/branching shape.

Now let's run the full end-to-end test one more time to confirm everything works through the real `peek_jni.cpp` pipeline, not just the isolated `jni_annotate` call.

Confirmed: `after_remove_writeonly=182` now (previously dropped to `78`), staying stable through every subsequent pass, all the way to the final `POST_ANNOTATE`. Both `is_even` and `is_odd` decompile fully and correctly through the complete, real pipeline.

Now let's strip out all the temporary diagnostic scaffolding (markers, dialogs, trace accumulators) since the actual bug is fixed and confirmed — time to ship the clean fix, not the debug harness.

Now remove the now-unused `pre_annotate_snapshot` variable and its declaration.

Now revert the diagnostic instrumentation in `decompiler_bridge.cpp` (the `inject_sig` tracing, call-survival checks) and `jni_annotator.cpp` (the per-pass trace), keeping only the real fix.

Now remove the call-survival checks and the `diag()`/`g_diag_trace` machinery.

Now remove `diag()`/`g_diag_trace`/`peek_get_diag_trace` and the `g_diag_trace.clear()` call.

Now remove the header declaration and the unused `<cstdarg>` include (keep it actually, since it's harmless and removing it risks nothing — I'll leave it; not worth the risk of breaking something for a cosmetic include cleanup). Let's remove the header declaration only.

## Root cause found, fixed, and verified

`remove_writeonly_vars` in `jni_annotator.cpp` had a too-loose check for "is this line a variable declaration": it treated any line with a semicolon and no `=` as a declaration. But `return (uint8)xStack_14;` also has a semicolon and no `=` — so the function's only real *read* of the variable got misclassified as a non-read, making the variable look write-only across the whole function. That triggers the merge/strip logic, which deletes the branch bodies that write to it. This explains every symptom from this entire investigation: it only hits functions with a cast on the return statement and multiple write sites (exactly `is_even`/`is_odd`'s shape), and it's completely independent of caching, build staleness, or the NDK toolchain — confirmed reproducible on x86_64 once I fed it the *exact* real text instead of a hand-typed approximation that happened to dodge it.

Fix: require the line not start with `return` before treating it as a declaration. Verified end-to-end through the real pipeline in my harness — both `is_even` and `is_odd` now decompile fully and correctly.

I've reverted most of the diagnostic scaffolding (markers, dialogs, trace accumulators in `peek_jni.cpp` and `decompiler_bridge.cpp`) but ran out of turn budget before finishing the cleanup in `jni_annotator.cpp` (the `g_jni_annotate_trace`/`peek_get_jni_annotate_trace` accessor is still there) and before re-verifying the fully-cleaned build compiles and packaging a zip. Tell me to continue and I'll finish stripping the remaining instrumentation, do a final clean-build verification, and ship it.